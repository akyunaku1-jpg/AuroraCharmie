# =========================
# Aurora Charmie - Production .htaccess
# =========================

Options -Indexes
ServerSignature Off

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Never expose sensitive files
  RewriteRule ^(\.env|\.env\.local|package\.json|package-lock\.json|server\.js)$ - [F,L,NC]

  # Keep real files/directories working
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # App routes
  RewriteRule ^$ index.html [L]
  RewriteRule ^index/?$ index.html [L,NC]
  RewriteRule ^catalog/?$ catalog.html [L,NC]
  RewriteRule ^info/?$ info.html [L,NC]
  RewriteRule ^order/?$ order.html [L,NC]
  RewriteRule ^admin/?$ admin.html [L,NC]
  RewriteRule ^admin-login/?$ admin-login.html [L,NC]
  RewriteRule ^admin-register/?$ admin-register.html [L,NC]
  RewriteRule ^admin-dashboard/?$ admin-dashboard.html [L,NC]
</IfModule>

<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
  Header always set Cross-Origin-Opener-Policy "same-origin"
</IfModule>

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/plain text/html text/css text/javascript application/javascript application/json application/xml image/svg+xml
</IfModule>

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/html "access plus 0 seconds"
  ExpiresByType text/css "access plus 7 days"
  ExpiresByType application/javascript "access plus 7 days"
  ExpiresByType image/png "access plus 30 days"
  ExpiresByType image/jpeg "access plus 30 days"
  ExpiresByType image/webp "access plus 30 days"
  ExpiresByType image/svg+xml "access plus 30 days"
  ExpiresByType image/x-icon "access plus 30 days"
</IfModule>
